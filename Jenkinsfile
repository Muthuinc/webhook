pipeline{
    agent any

    stages{
        stage ('test') {
            steps{
                echo "dev branch"
                echo "second line"
                echo "third line for pull request"
            }
        }
    }
}